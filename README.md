# Janovec-lekce-git

Toto je testovací repozitar

git config --global user.name ""
git config --global user.email ""

git status - zobrazi zmennene a stage soubory
git diff - ukaze konkretni zmeny
git add . (tam musi byt mezi teckou a add mezera) pridavame soubory
git commit -m "" tím zmeny potvrdime a davema zpravu kdo a kdy to udelal
